# Environment Variables for Team Scheduler

# API Base URL for backend service
# In development, use empty string (defaults to proxy in package.json)
# In production (Render), use your deployed backend URL
# Example: https://team-scheduler-api.onrender.com
REACT_APP_API_URL=
